# Per-device bootstrap secrets are provisioned in Google Cloud Secret Manager.
# Do not store any actual bootstrap secret in this file or in RTDB.
# The deployed Functions runtime needs permission to access
# projects/<project>/secrets/smartflow-bootstrap-<lowercase-device-id>.
