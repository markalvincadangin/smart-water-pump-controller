#pragma once

// Shared header for the PlatformIO firmware project.
// Copied from `src/smart_water_pump_controller_shared.h` so that all .ino/.cpp
// files can simply `#include <smart_water_pump_controller_shared.h>`.

#include "../src/smart_water_pump_controller_shared.h"

