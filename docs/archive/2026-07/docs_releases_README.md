## Releases

This directory contains **release documentation sets** for deployment-ready snapshots.

Current stable release:

- `v1.0.0/` (first stable release)

The current, source-of-truth specifications live under:

- `docs/specs/`

