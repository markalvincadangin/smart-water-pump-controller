## Specs (current)

These are the **current, non-versioned** specifications for the system as implemented in this repository.

- `firmware.md`: ESP32 + ESP8266 firmware spec, RS‑485 contract, safety model, RTDB fields
- `dashboard.md`: dashboard spec, RTDB contract usage, control UX rules

Historical documents remain under:

- `docs/archive/` (superseded drafts and older internal docs)
- `docs/archive/releases/` (release-specific historical notes)

