## Releases

This directory contains **release documentation sets** for deployment-ready snapshots (e.g. `v1.0.0/`).

The current, source-of-truth specifications live under:

- `docs/specs/`

Historical pre-release/versioned documents have been moved to:

- `docs/archive/releases/`

