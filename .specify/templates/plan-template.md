# Implementation Plan: [FEATURE]

**Branch**: `[###-feature-name]` | **Date**: [DATE] | **Spec**: [link to spec.md]

**Input**: Feature specification from `/specs/[###-feature-name]/spec.md`

**Note**: This plan is generated by `/speckit-plan`. See `.specify/templates/plan-template.md` for the execution workflow.

---

## Summary

[Extract from feature spec: primary requirement + technical approach from research]

---

## Constitution Gate *(must pass before Phase 0)*

| Principle | Status | Evidence |
|-----------|--------|----------|
| I. Fail Toward Pump OFF | ☐ Pass / ☐ N/A | [describe fault paths or "not applicable"] |
| II. Dry-Run Lockout | ☐ Pass / ☐ N/A | |
| III. Overflow Protection | ☐ Pass / ☐ N/A | |
| IV. TOR Independence | ☐ Pass / ☐ N/A | |
| V. Sensor Freshness / E-Stop | ☐ Pass / ☐ N/A | |
| VI. Backward Compatibility | ☐ Pass / ☐ N/A | |

*Re-check this gate after Phase 1 design completes.*

---

## Technical Context

**Modules affected** *(check all that apply)*:
- [ ] Firmware Master — `firmware/platformio_smart_water_pump_controller/`
- [ ] Firmware Sensor Node — `firmware/platformio_sensor_node/`
- [ ] Dashboard — `dashboard/` (Next.js 15, TypeScript, TailwindCSS, Firebase SDK)
- [ ] Cloud Functions — `functions/` (TypeScript, Firebase Functions v7)
- [ ] Admin Scripts — `scripts/`
- [ ] Docs only — `docs/`

**Firebase RTDB impact**: [Additive schema change / No RTDB change / NEEDS CLARIFICATION]

**RS-485 protocol impact**: [Frame change (backward compat required) / No change / NEEDS CLARIFICATION]

**Hardware required for full validation**: [Yes — describe setup / No]

**Performance / timing constraints**: [e.g., "RS-485 response window ≤ 200ms" / N/A]

---

## Project Structure (this feature)

### Spec-Kit Artifacts

```text
specs/[###-feature]/
├── spec.md              # Feature specification
├── plan.md              # This file
├── research.md          # Phase 0 output
├── data-model.md        # Phase 1 output (if RTDB schema changes)
├── quickstart.md        # Phase 1 output
├── contracts/           # Phase 1 output (API / RTDB contracts)
└── tasks.md             # Phase 2 output (/speckit-tasks — NOT created here)
```

### Source Code Paths

<!--
  Replace with the concrete paths touched by this feature.
  Delete module sections that don't apply.
-->

```text
# Firmware Master (if applicable)
firmware/platformio_smart_water_pump_controller/src/
├── main.cpp             # State machine, pump control, watchdog
├── rs485_master.cpp/.h  # RS-485 frame encoding/decoding
└── firebase_control.cpp # RTDB polling, setPump() gate

# Firmware Sensor Node (if applicable)
firmware/platformio_sensor_node/src/
├── main.cpp             # Sensor loop, RS-485 slave response
└── sensors.cpp/.h       # JSN-SR04T, YF-G1 readings

# Dashboard (if applicable)
dashboard/src/
├── app/                 # Next.js pages and API routes
├── components/          # React components
└── lib/                 # Hooks (usePumpData, usePendingControl) and utilities

# Cloud Functions (if applicable)
functions/src/
└── index.ts             # Trigger handlers (RTDB, HTTP, scheduled)

# Tests
dashboard/__tests__/     # Jest + @testing-library/react
functions/src/__tests__/ # Jest
```

---

<!-- ============================================================
  MODULE IMPLEMENTATION PHASES
  Include only phases for modules this feature actually touches.
  Delete inapplicable phase blocks entirely.
============================================================ -->

## Phase 0: Research & Spike

**Purpose**: Understand current behavior before designing changes

- [ ] Read relevant `docs/specs/` files
- [ ] Trace current implementation in affected source files
- [ ] Identify all call sites that would change
- [ ] Confirm constitution gate (above) can be satisfied
- [ ] Output: `research.md`

---

## Phase 1: Design

**Purpose**: Finalize contracts and schema before writing implementation code

<!--  Firmware sub-phase — include if firmware is touched -->
### Phase 1A: Firmware Design *(if applicable)*
- [ ] Define any new RS-485 frame fields (name, byte offset, type, default if absent)
- [ ] Map state machine transitions and relay disposition for each new path
- [ ] Confirm all new timing uses `elapsedMillis32()` / `millisDeadlineReached()`
- [ ] Output: `contracts/rs485_frame_delta.md`

<!-- Firebase sub-phase — include if RTDB or Functions are touched -->
### Phase 1B: Firebase Contract Design *(if applicable)*
- [ ] Define additive RTDB schema nodes (JSON snippet with types and writer/reader owners)
- [ ] Confirm no existing node is renamed or removed
- [ ] Define Cloud Function triggers and idempotency behavior
- [ ] Output: `contracts/rtdb_schema_delta.md`

<!-- Dashboard sub-phase — include if dashboard is touched -->
### Phase 1C: Dashboard Design *(if applicable)*
- [ ] Wireframe or describe UI state changes
- [ ] Identify which hooks/components change (`usePumpData`, `usePendingControl`, etc.)
- [ ] Define offline behavior
- [ ] Output: `quickstart.md` section for dashboard changes

**Checkpoint**: Constitution re-check — all design outputs reviewed against §IV principles

---

<!-- Firmware implementation phase — include if firmware is touched -->
## Phase 2: Firmware Implementation *(if applicable)*

**Build command**: `cd firmware/platformio_smart_water_pump_controller && pio run`
**Sensor node**: `cd firmware/platformio_sensor_node && pio run`
**Validation**: Hardware serial monitor — document expected log output (no CI automation available)

- [ ] [FW-001] [describe task — be specific to file and function]
- [ ] [FW-002] Verify `setPump()` is the only relay control entry point (grep check)
- [ ] [FW-003] Verify no raw `millis()` arithmetic introduced (grep check)
- [ ] [FW-004] Document expected serial output for manual hardware validation

**Checkpoint**: Firmware builds clean, relay disposition verified against constitution

---

<!-- Dashboard implementation phase — include if dashboard is touched -->
## Phase 3: Dashboard Implementation *(if applicable)*

**Dev server**: `cd dashboard && npm run dev` (port 3000)
**Test**: `cd dashboard && npm test`
**Validate**: `cd dashboard && npm run validate` (lint + build + lighthouse)

### Tests (write first — must FAIL before implementation)
- [ ] [DB-T001] [Describe test in `dashboard/__tests__/`]

### Implementation
- [ ] [DB-001] [describe task — file path and what changes]
- [ ] [DB-002] [describe task]

**Known pre-existing failure**: `__tests__/lib/usePumpData.test.tsx` (4 tests, mock/update mismatch) — confirm this feature does not worsen the count.

**Checkpoint**: `npm test` passes (minus pre-existing 4), `npm run validate` clean

---

<!-- Cloud Functions phase — include if functions are touched -->
## Phase 4: Cloud Functions Implementation *(if applicable)*

**Build**: `cd functions && npm run build`
**Test**: `cd functions && npm test`

### Tests (write first — must FAIL before implementation)
- [ ] [FN-T001] [Describe test in `functions/src/__tests__/`]

### Implementation
- [ ] [FN-001] [describe task — function name and trigger]
- [ ] [FN-002] [describe task]

**Checkpoint**: `tsc` clean, `npm test` passes

---

## Phase 5: Integration & Polish

**Purpose**: Verify cross-module behavior and update documentation

- [ ] End-to-end test: [describe the observable behavior across modules]
- [ ] Update `docs/specs/` if this feature changes a normative spec
- [ ] Update `docs/operations/` if this changes operational runbook steps
- [ ] Commit with Conventional Commits message: `feat([scope]): [description]`

---

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| [e.g., direct RELAY_PIN write] | [current need] | [why setPump() insufficient] |
